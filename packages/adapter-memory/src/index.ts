/**
 * @ltikit/adapter-memory — in-memory stores for dev + tests.
 *
 * Phase 0: scaffold only. `MemoryNonceStore` / `MemoryPlatformStore`
 * implementations land in Phase 2 once the core adapter interfaces exist.
 */
export const version = '0.0.0'
