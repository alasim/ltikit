---
"@ltikit/core": minor
---

Add `signCapabilityLink`/`verifyCapabilityLink` — a self-issued, self-verified short-lived token using the tool's own keypair, for no-login privileged links (e.g. a faculty review link opened from Canvas SpeedGrader). Add `canvasSubmission` to `publishScore`/`postScore` — the Canvas-only AGS score extension (`https://canvas.instructure.com/lti/submission`) that attaches a clickable review link to a posted grade.
